{
  "name": "web-game",
  "engines": {
    "node": "17.x"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start-dev": "npm run start-dev --prefix src/client/",
    "build": "npm run build --prefix src/client/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.2",
    "shared": "file:src/shared/",
    "web-client": "file:src/client/",
    "ws": "^8.4.2"
  },
  "type": "module",
  "devDependencies": {
    "eslint": "^8.6.0"
  }
}
